<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg2"
    tools:context=".MainScreen">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageButton
            android:id="@+id/playbut"

            android:layout_width="291dp"
            android:layout_height="143dp"
            android:layout_gravity="center"
            android:layout_marginStart="4dp"
            android:layout_marginTop="430dp"
            android:background="@drawable/play"
            android:contentDescription="@string/playy"
            android:onClick="goGameScreen"
            tools:ignore="DuplicateSpeakableTextCheck" />

        <ImageButton
            android:id="@+id/qusetmark"
            android:layout_width="126dp"
            android:layout_height="89dp"
            android:onClick="helps"
            android:layout_gravity="center"
            android:layout_marginStart="4dp"
            android:background="@android:color/transparent"
            app:srcCompat="@drawable/quset"
            android:contentDescription="@string/qusetmark" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>